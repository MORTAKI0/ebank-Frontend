function LoginPage() {
  return (
    <div>
      <h1>Login (Sprint 0)</h1>
      <p>
        In Sprint 1 this page will contain the real login form that calls the Spring Boot
        <code> /auth/login </code> endpoint and stores the JWT token.
      </p>
    </div>
  );
}

export default LoginPage;
